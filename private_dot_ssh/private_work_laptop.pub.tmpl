{{- if (not (or (eq .targetname "nessie") (eq .targetname "cooper"))) -}}
{{- if (not (contains .chezmoi.hostname .work.hostnames.mac_laptop)) -}}
{{ onepasswordRead .op.urls.work_laptop }}
{{ end }}
{{- end -}}
